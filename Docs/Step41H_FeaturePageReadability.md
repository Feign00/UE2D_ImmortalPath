# 第 41H 部分：宗门、灵田、炼丹与炼器内部页可读性

日期：2026-09-08。

## 本批范围

- 短按钮文字关闭自动换行，内容横向填充、垂直居中；保留手工指定的两行标题。极长标题使用省略号，炼器条目悬停可看完整名称。
- 宗门选择说明、任务说明、兑换说明，以及灵田作物说明、田块详情和结果提示，改为有边界的独立滚动区。文本不删减，不覆盖相邻按钮。
- 炼丹和炼器移除横向拉伸的旧背包底框，使用现有深色玉金风格；丹方和炼器条目保留选中、悬停、按下和禁用状态。
- 炼丹材料、药效及结果说明可滚动，丹药背包增加纵向滚动。概率数值不变，丹房加成解释移至悬停提示。
- 炼器装备名称收窄至独立详情列，避免伸入强化消耗列；配方条目宽度由 250 改为 220，容纳列表滚动条。强化、洗炼成本与长说明可滚动。
- 原生点击还发现列表重建当帧的高度尚未计算，会短暂重叠。重建丹方、丹药和炼器列表后立即进行布局预计算；独立检查增加“刷新当帧行高非零”，不只验收下一帧恢复后的截图。
- 共用滚动布局保留位置、尺寸和层级，明确裁切边界，重复应用不会嵌套。未改变战斗、修炼、资源、宗门加入或存档规则。

## 验证与安全

- 新增 `ImmortalPath.UI.FeatureButtonLabels` 与 `ImmortalPath.UI.FeatureBoundedDetails`，覆盖按钮自动换行、明确换行保留、对齐、选中风格、滚动边界、层级及完整文本。
- 非 Shipping 独立预览开关 `-ImmortalTestFeatureLayoutPreview` 只接受项目 `Saved/Automation/DesktopPanels` 下的 `-UserDir`。为测试背包滚动加入普通/极品丹药，不改正式存档；此开关不自动点击或退出。
- 首轮编译暴露两个错误的炼器成本成员名，已修正为现有 `EnhancementCostText` / `RefinementCostText`。后续重新构建，不忽略失败。
- 最终 `ImmortalPathEditor` 与 `ImmortalPath` Win64 Development 构建通过。完整 `ImmortalPath.` 自动化 **54 成功、0 警告、0 失败、0 未运行**；报告保留于忽略目录 `Saved/TestReports/LayoutFinal41H`。
- 独立 DX11 管理页流程 **14 项检查全部通过**：展开位置、战斗坐标、八页切换期间自动攻击计时器、独立飞升、收起恢复，以及炼丹/炼器刷新当帧的行高。日志 `Saved/Logs/LayoutFinal41HIntegration.log`；这是程序化检查，不冒充鼠标点击。
- 通过电脑操作工具进行了真实原生点击：炼丹选择回血丹/聚气丹、十种丹药堆叠滚到末行并选择极品聚气丹、滚动查看完整药效；炼器点击青云剑并确认修复后列表不再瞬间堆叠；宗门选择天剑宗；灵田选择仙果并确认未达到等级时操作按钮禁用；主页返回与菜单收起。没有在正式存档中加入宗门、服药或购买资源。
- 2560 像素宽、150% Windows 缩放下，紧凑与展开状态原生透明合成检查的 `sourceKey/nativeKey/desktopVisible/foregroundVisible` 均为 true。收尾运行额外验证了收起后的合成、Alt+F4 正常退出（进程退出码 0）及窗口实际消失。原生截图包含私人桌面，仅用于本地检查，不提交到仓库。
- 正式存档测试前后 SHA256 均为 `98DE75CD856463348A4547C4D7F5D202613A0EA984CB41CA675FB6F1FEF61579`。

## 尚未完成

本批是可读性和部分视觉统一，不是四个功能页最终美术验收。文字主导的详细信息仍保留；更多图形化内容、其他内部页、怪物/宠物/Boss 新动作以及分地图植被仍需后续分批完成。

宗门/灵田部分说明在当前缩放下仍偏小，后续应结合版式重新分配空间；不能仅靠继续缩小文字。原生观察中个别旧圆环特效仍出现洋红色块，需另批核查特效素材透明通道，本批没有修改或验收其素材。不同 DPI 的布局数学回归仍覆盖 1280/1707/2560 宽度，实际鼠标/桌面视觉仅验收上述当前屏幕配置，未声称所有显示器实测完成。
