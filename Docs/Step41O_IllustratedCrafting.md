# 第 41O 部分：炼器大面板接入与验收

日期：2026-09-13。接续已提交的炼器素材准备，完成本页图标接入与交互验收。

## 范围

- 炼器页由 1600×270 重排为 1600×600；配方、打造台、装备列表、强化/洗炼分区，沿用管理界面的深墨绿底板和品质颜色。
- 九种装备部位按显式枚举映射图集；兼容旧存档的靴子与项链枚举顺序，不直接将枚举值当作图格编号。配方及装备行使用 56×56 图标和 88 高行，所选产物/装备另有大预览。
- 材料图标、持有/所需数量、动态资源不足状态；矿石和灵石复用炼丹材料图集，妖骨、灵铁、法宝残片、灵木使用新图集。长说明、词条和多材料列表保留滚动访问，固定操作按钮不被挤走。
- 锁定配方可查看，但打造仍使用原有关卡与资源校验；首次默认选最低关卡需求的有效配方。未知图标不借用别的物品图，缺图时保留名称与数值。
- 资源充足但储物戒无法容纳/替换时，明确提示“储物戒已满，请先整理”，不再误报材料不足。
- 战斗货币变化不再反复销毁配方/装备行；关卡、装备或选择变化才重建对应行。修正详情标题重复显示强化等级的问题。
- 未改打造、强化、洗炼成本及数值计算，未改战斗/修炼、存档或飞升规则；普通菜单不暂停战斗。

## 验证

- 最终 Development Editor 与 Game 双目标编译成功。
- 完整自动化 `ImmortalPath.`：64 成功、0 警告、0 失败、0 未运行，最终报告 `Saved/TestReports/Crafting41ORelease/index.json`（目录名 Release 仅标识本次最终检查，不是 Shipping 打包）。新增图格映射、实际 418 像素格宽、配方覆盖、未知 ID 回退及三张资源加载/压缩/驻留检查。
- 首轮结果为 63 成功、1 失败：三等分的浮点差异使 4 个图格的默认极小容差比较失败；改为实际像素宽高均为 418，容差 0.01 像素，再跑完整套件通过。未删除失败测试，也未改动图格大小掩盖错误。
- DX11 独立运行最终流程 44 项通过、0 失败，日志 `Saved/Logs/Crafting41OReleaseFlow.log`。新增检查 1600×600 页面、图标、大列表行、18 号以上文字与边界、空装备禁用且无残留图、资源不足禁用、满强化等级的禁用和满级标记、全锁定满背包且材料充足时的正确提示。该夹具同步替换并恢复隔离测试数据；满级标记检查不等同于实际连续强化 15 次，满背包场景由自动流程验证而非手动锁定每一格。

## 原生鼠标测试

第一轮：独立 `Crafting41ONative` 测试目录，赠送炼器材料与灵石，布局预览提供无敌保护。游戏视口日志最终为 2560×1024。

- 实际打造青云剑：矿石 50→46、妖骨 50→49，新装备自动穿戴并被选中；日志记录 2 条词条，灵石结算后 4931。
- 实际强化：矿石 46→44，战力 35.84→38.52，等级 +1，灵石结算后 4910。
- 实际洗炼：妖骨 49→46，洗炼次数 0→1，产生 2 条新词条；日志确认基础攻击 6.72→6.72 未被洗炼误改，灵石结算后 4801。
- 战斗同时产生奖励，因此不使用隔数秒截图中的灵石差值当作精确成本证据；以上结算灵石来自各操作日志。
- 查看第 10 关锁定配方，图标、描述和材料切换，打造显示“关卡未解锁”；配方列表滚动、关闭回洞府均正常。
- 此轮发现标题重复“+1”，修正后重新双目标编译和完整测试。

最终轮：独立 `Crafting41OFinalNative`，满列表/长名称预览，不赠送炼器材料。

- 长名称在详情区换行显示，列表用省略与提示保留访问；选择护甲时衣服图标、属性和词条正确切换，标题不再重复强化等级。
- 初始材料不足，点击禁用打造按钮不发生打造；多材料洗炼列表可滚动查看第三项灵铁。
- 返回洞府 → 点击炼器坊重新进入，选择与滚动位置仍可使用；后台战斗继续。
- 自然掉落补足灵石和矿石后，强化按钮由禁用转可用；实际强化护甲，战力 36.86→39.24，矿石 2→0，灵石结算后 7，标题只显示一次 +1，资源不足后按钮再次禁用。
- 收起恢复透明底部战斗条，Alt+F4 正常退出，两个手动测试进程均返回 0。

所有测试使用 `Saved/Automation/DesktopPanels` 下的独立 UserDir；正式存档 SHA256 前后均为 `98DE75CD856463348A4547C4D7F5D202613A0EA984CB41CA675FB6F1FEF61579`。原生截图含其他桌面内容，不保存或上传；日志与自动截图留在本地忽略目录。

## 边界与下一批

- 本次只完成炼器页；未替换背包全局装备图标。独立法宝炉仍沿用已有页面，未声称其美术重做完成。
- 仍可见旧演员/法宝的洋红边缘和块状背景，未在本次修复；不将正常退出等同于全部退出粉屏场景通过。
- 长配方说明、多词条和高阶洗炼的多材料仍需在各自区域滚动，不承诺所有内容同屏无滚动。
- 原创装备图保留生成 alpha，原生尺寸可辨；本次未完成全分辨率视觉矩阵或打包发行版验证。
- 下一批继续百宝阁商品/详情界面，之后处理飞升旧小面板与剩余功能页、全演员及分地图植被美术。总体目标仍未完成。
