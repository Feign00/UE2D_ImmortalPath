# 第 41M 部分：宗门徽记与大面板

日期：2026-09-11。本批宗门徽记、大面板及交互验收完成。保留现有宗门规则，改造展示与交互空间。

## 范围

- 宗门页由 1600×270 改为 1600×600。左侧四宗门徽记选择、门槛和入宗；中间三张每日任务卡及进度条；右侧四张宝库图标卡、消耗/条件说明与独立兑换按钮。
- 四枚原创徽记使用内置 image_gen 生成。实际输出 1254×1254，保留原始 alpha，按 2×2 格和宗门 ID 映射，不依赖目录顺序。无图片或未知宗门时不显示错误徽记，名称保留。
- 任务与宝库的小图标复用现有代码像素符号，不宣称这七个图标为新生成素材。任务条显示实际进度，未入宗为零、已领取转暗金；长说明可滚动，短操作按钮不自动换行。
- 补充新 UI 图集目录的明确打包包含配置；本批未执行完整打包，不将配置和资源加载测试当作发行包验证。
- 不改入宗门槛、不可改投规则、贡献/每日上限、领取及兑换事务、修炼和飞升动画；普通菜单不暂停历练。

## 验证

- Development Editor / Game 双目标最终编译成功。
- 完整 `ImmortalPath.` 测试：60 成功、0 警告成功、0 失败、0 未运行。新增 ID/UV/缺失回退/进度边界与实际图集加载检查。本地报告 `Saved/TestReports/Sect41M/index.json`。
- DX11 隔离管理流程 31 项通过、0 失败，包括四张徽记、三根有效进度条、600 高内容、控件边界、切页继续战斗和关闭恢复紧凑窗口。日志 `Saved/Logs/Sect41MIntegration.log`。
- 第一轮编译中测试对 UE 5.7 `GetUVRegion()` 的返回包装类型误用 `auto`，已显式转换为 `FBox2f`，不关闭诊断或删掉 UV 检查。
- 首次生图请求网络错误；重试后输出已在本机产生。恢复工作时原异步句柄消失，依据实际文件确认产出，没有重复生成。导入 1 纹理、1 资源包、0 错误/警告。

## 原生实测记录

所有进度使用 `Saved/Automation/DesktopPanels` 内隔离 UserDir。原生 DX11，2560 像素宽桌面、150% 系统缩放。

- 无徽记时只进行布局预检，未当作美术完成。导入后观察四枚徽记无黑底/棋盘格、对应图案可区分。
- 实际点击天剑宗、万妖谷、魔宗，门槛与禁用入宗状态正确；切回青云宗并加入，身份更新为外门弟子，宝库与任务展示打开，不可改投按钮禁用。
- 加入后实际战斗使除妖进度从 0/20 增加到 2/20，历练进度到 1/3，进度条分别显示约十分之一与三分之一。长说明鼠标滚动后可读到不可退宗/改投的末行。
- 使用 `Native41MComplete` 隔离存档真实点击加入青云宗，然后正常退出并重启；已加入状态保留。第二轮通过已有开发参数补足三项任务和 1000 贡献，仅用于操作验收，不写入正式存档。
- 真实点击三项领取：贡献 1000→1060→1150→1270；三个按钮逐个变为“今日已领取”，条形进度保持满格并转暗金，头部为 3/3 已领取。
- 真实点击宗门物资、悟道玉简、宗门真传：贡献 1270→1230→1080→580。物资今日 1/5、玉简 1/2；真传显示已经兑换并禁用，按钮显示“功法已领悟”。
- 真实点击灵石俸禄三次：贡献 580→500→420→340，各次获得 250 灵石（战斗掉落仍在增加灵石，精确奖励以事务日志为准）；今日 3/3 后按钮显示“今日已达上限”并禁用。
- 返回洞府、点击宗门建筑重新进入，任务/次数/贡献状态保留；点击收起恢复透明紧凑战斗条，两轮测试均用 Alt+F4 正常退出，进程返回 0。没有使用 UI 自动化操作背景应用。
- 所有最终操作与事务记录一致，详见本地 `Saved/Logs/Sect41MComplete.log`、`Sect41MRewards.log`。截图含桌面背景，不上传。

正式主存档前后 SHA256 均为 `98DE75CD856463348A4547C4D7F5D202613A0EA984CB41CA675FB6F1FEF61579`。原生实测仍可见旧法宝紫红底块和旧怪物/宠物边缘，这些不属于本批已完成内容。

## 美术总目标的边界

宗门徽记与页面不是整套人界美术。炼丹/炼器、百宝阁、飞升内部页等继续分批图形化；怪物、宠物、Boss 完整动作族、分地图局部植被、旧紫红边缘特效仍未完成。正式存档、桌面截图、日志和编译产物不入库。

完整提示词与源文件见 [宗门素材记录](../ArtSource/MortalRealm/DesktopPixelV2/Sect/README.md)。
