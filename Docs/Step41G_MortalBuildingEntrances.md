# 第 41G 部分：人界建筑式功能入口

日期：2026-09-08。状态：本批建筑入口接入完成，不是全部功能页美术完成。

## 本批范围

- 洞府主页六个入口使用原创建筑图集：修炼静室、宗门大殿、炼丹房、炼器坊、洞府营造、灵田。
- 坊市使用独立百宝阁建筑；仍只进入已有购买/出售页面，不新增玩法或货币规则。
- 延续 41F 的局部面板、金色细边、短标签和底部实时战斗。建筑图片不接收鼠标，整张入口卡片响应点击；按钮内容明确填充容器，修复宽卡片内容偏右。
- 人界建筑通过软引用登记，缺图时保留原图标；非人界恢复原图标和对应原有场景，不将本批素材冒充其他阶段美术。
- 原图、导入清单、生成提示词均入库，旧资源和飞升动画不改动。内部功能页的正文布局与美术仍待后续批次。

## 素材验收

使用内置 image_gen，最终路径及提示词见 [素材记录](../ArtSource/MortalRealm/DesktopPixelV2/Management/README.md)。最终两张 PNG 都是 1536×1024 的 RGBA 图，直接复制原始输出到项目，不做程序抠图或改色。

曾误把原始预览的暗色显示当成不透明背景；对源文件复核后确认洞府图包含 617,889 个 alpha=0 像素，百宝阁包含 666,519 个 alpha=0 像素。游戏中可透明叠加。额外的背景编辑结果是带棋盘格的 RGB 图，未采用；因此不再需要此前的程序抠图确认。保留生成的柔和透明边缘，未宣称所有边缘都是硬二值像素。

## 验证过程

- 项目导入：2 张贴图、2 个资源包，0 错误、0 警告；TC_EditorIcon 无损压缩、无 mip、常驻，导入来源在项目内。
- 首轮完整测试 51 成功、1 失败：NullRHI 下 GPU 资源尺寸是 0。资产测试和运行时插画比例改为读取导入尺寸，不依赖 GPU 资源初始化时机，未跳过尺寸验证。
- 首轮独立建筑入口检查共 18 项全部通过，涵盖七张插画存在、七个实际按钮委托进入对应页面、窗口展开/收起、演员锚点、飞升与战斗继续。该委托检查不是原生鼠标点击证明，另行进行桌面点击。

- 最终 Development Editor 与 Game 构建均成功；完整 `ImmortalPath.` 报告 `Saved/TestReports/Buildings41GFinal/index.json` 为 52 成功、0 警告成功、0 失败、0 未运行，进程退出码 0。
- 最终独立运行 `BuildingsFinal41G` 的 18 项检查全部通过、`failures=0`、退出码 0。窗口仍从 2560×320 向上展开至 2560×680，保留演员桌面锚点；独立截图经查看确认图集格子正确、百宝阁居中。
- 通过桌面检查技能实际点击全部七个建筑入口：修炼、宗门、炼丹、炼器、洞府、灵田、百宝阁；同时验证主页导航和收起。日志七次对应页面均为 `combatPaused=false`、`autoAttackActive=true`，没有执行购买、出售、炼制或升级操作。
- `Native41G` 的 DX11 原生合成检查在紧凑、展开状态下四项指标均为 true；实际桌面可见面板外围与底部战斗，插画无棋盘底，通知未挡住入口。
- 实测结束后收起并用 Alt+F4 关闭隔离游戏，退出码 0、窗口已销毁；正式存档校验值与测试前一致。

## 边界

这是七个主页入口的静态建筑美术接入，不是整套洞府/坊市或全部功能页美术完成。实际查看发现旧宗门、灵田、炼丹/炼器等详细页仍有文字拥挤、窄按钮换行和局部裁切；后续优先重排这些正文与操作区，不将其算作本批已解决。角色养成、历练、详细背包/商店/修炼页的进一步美术，以及怪物、宠物、Boss、地图植被仍未包含在本批。未进行打包烘焙或云端 UE 编译。

所有运行使用 `Saved/Automation/DesktopPanels` 下的隔离 UserDir；正式主存档 SHA-256 为 `98DE75CD856463348A4547C4D7F5D202613A0EA984CB41CA675FB6F1FEF61579`。原生桌面截图、日志、测试存档和编译产物不入库。
