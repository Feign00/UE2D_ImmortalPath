# 开发与交付约定

- 按用户要求分阶段交付；一次只声明经过验证的具体部分完成。动画样稿、素材接入和实机动画验收分别记录。
- 每个独立部分完成后由开发代理自行测试、审查差异、提交并推送到已配置的 GitHub 远程；不默认要求用户代测。
- 修改前检查工作区、当前分支和远程状态，保护已有改动；提交只包含当前部分及必要依赖，不混入无关素材或用户文件。
- C++ 运行时代码修改需编译 Development Editor 与 Game 目标，运行 `ImmortalPath.` 自动化测试。原生窗口、UI、动画修改还需要相应独立运行验证；自动化通过不等同于视觉验收通过。
- 所有游戏测试使用独立 `-UserDir`，不得用正式存档试验破坏性操作。涉及存档时记录正式存档校验值与测试前后结果。
- 在 `Docs` 记录范围、验证方式、结果与已知限制；失败必须如实记录。未完成美术不得标记为已完成。
- 提交前检查暂存差异、空白错误、密钥和大文件；禁止上传凭据、个人存档、缓存、临时截图、日志和编译产物。
- 提交信息使用明确的 `feat(...)`、`fix(...)`、`refactor(...)`、`test(...)` 或 `docs(...)` 描述，不再使用泛化的 `Update project`。
- 推送前拉取远程引用并确认无分叉；不强推、不自动改写历史。出现冲突先保留当前工作并解释阻碍。
- 推送后验证远程提交号，向用户报告本部分、测试结果和 GitHub 提交链接。未确认远程成功时不得声称已上传。
- 当前人界美术重制完成前，不主动扩展灵界/仙界玩法；保留独立飞升动画，战斗不提供修为。

详细流程见 `Docs/DevelopmentWorkflow.md`。
